import { Divider } from 'foundation/components/kit';
import React from 'react';

const Separator: React.FC = () => (
  <Divider direction="row" backgroundColor={'gray'} height={1} />
);

export default Separator;
